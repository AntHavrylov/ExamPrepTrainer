# Set to the deployed backend's origin when the frontend is built for a
# different-origin deploy (e.g. GitHub Pages calling an Azure-hosted
# backend). Leave unset for local dev (uses the Vite dev proxy in
# vite.config.js) and same-origin deploys (Nginx-proxied Docker).
VITE_API_BASE_URL=
